services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bookme
    ports:
      - "5433:5432"
    volumes:
      - bookme_pg_data:/var/lib/postgresql/data
volumes:
  bookme_pg_data:
